{% extends 'base.html.twig' %}

{% block body %}
    <div class="app-shell">
        <aside class="sidebar">
            <div class="sidebar-brand">
                <img src="{{ asset('images/R-Logo.png') }}" alt="R Logo">
            </div>

            <nav class="nav-list">
                <a class="nav-link {{ app.request.attributes.get('_route') == 'app_dashboard' ? 'active' : '' }}" href="{{ path('app_dashboard') }}">
                    Dashboard
                </a>
                <a class="nav-link {{ app.request.attributes.get('_route') in ['app_profile', 'profile_availability', 'profile_requests'] ? 'active' : '' }}" href="{{ path('app_profile') }}">
                    Profile Account
                </a>
                {% if is_granted('ROLE_SUPER_ADMIN') %}
                <a class="nav-link {{ app.request.attributes.get('_route') == 'app_account_management' ? 'active' : '' }}" href="{{ path('app_account_management') }}">
                    Account Management
                </a>
                <a class="nav-link {{ app.request.attributes.get('_route') == 'app_facility_management' ? 'active' : '' }}" href="{{ path('app_facility_management') }}">
                    Facility Management
                </a>
                <a class="nav-link {{ app.request.attributes.get('_route') == 'admin_reservations' ? 'active' : '' }}" href="{{ path('admin_reservations') }}">
                    Reservations
                </a>
                <a class="nav-link {{ app.request.attributes.get('_route') starts with 'analytics_' ? 'active' : '' }}" href="{{ path('analytics_dashboard') }}">
                    Analytics
                </a>
                <a class="nav-link {{ app.request.attributes.get('_route') starts with 'mentoring_' ? 'active' : '' }}" href="{{ path('mentoring_admin') }}">
                    Mentoring
                </a>
                {% else %}
                <a class="nav-link {{ app.request.attributes.get('_route') == 'app_facility_index' ? 'active' : '' }}" href="{{ path('app_facility_index') }}">
                    Facility
                </a>
                <a class="nav-link {{ app.request.attributes.get('_route') == 'user_reservations' ? 'active' : '' }}" href="{{ path('user_reservations') }}">
                    My Reservations
                </a>
                <a class="nav-link {{ app.request.attributes.get('_route') starts with 'mentoring_' ? 'active' : '' }}" href="{{ path('mentoring_index') }}">
                    Mentoring
                </a>
                {% endif %}
                <a class="nav-link {{ app.request.attributes.get('_route') starts with 'research_' ? 'active' : '' }}" href="{{ path('research_index') }}">
                    Research
                </a>
            </nav>


            <div class="sidebar-footer">
                <div class="pill-role">
                    {{ is_granted('ROLE_SUPER_ADMIN') ? 'SUPER ADMIN' : 'USER' }}
                </div>
                <div>Signed in as {{ app.user ? app.user.userIdentifier : 'Guest' }}</div>
                <div><a href="{{ path('app_logout') }}">Log out</a></div>
            </div>
        </aside>

        <main class="content-area">
            {% block content %}{% endblock %}
        </main>
    </div>
{% endblock %}

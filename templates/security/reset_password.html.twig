{% extends 'base.html.twig' %}

{% block title %}Reset Password | Reserva FTIC{% endblock %}

{% block body_class %}page-login{% endblock %}

{% block body %}
    <div class="login-card">
        <div class="login-brand">
            <img src="{{ asset('images/Reserva-Logo.png') }}" alt="Reserva Logo">
        </div>

        <h2>Reset Password</h2>

        {% if errors|length > 0 %}
            <div class="alert alert-danger">
                <ul>
                    {% for error in errors %}
                        <li>{{ error }}</li>
                    {% endfor %}
                </ul>
            </div>
        {% endif %}

        <form method="post" action="{{ path('app_reset_password', {'token': token}) }}" data-turbo="false">
            <div class="form-group">
                <label for="password">New Password</label>
                <input type="password" id="password" name="password" required>
                <small style="display: block; margin-top: 6px; color: #666;">
                    Password must be 8+ chars with uppercase, lowercase, number, special char. Cannot reuse old password.
                </small>
                <div id="password-strength" style="margin-top: 5px;">
                    <div class="strength-bar">
                        <div id="strength-fill"></div>
                    </div>
                    <span id="strength-text">Enter password</span>
                </div>
            </div>

            <div class="form-group">
                <label for="passwordRepeat">Confirm Password</label>
                <input type="password" id="passwordRepeat" name="passwordRepeat" required>
            </div>

            <input type="hidden" name="_csrf_token" value="{{ csrf_token('reset_password') }}">

            <button class="btn-primary" type="submit">Change Password</button>
        </form>
    </div>

    <style>
        .strength-bar {
            width: 100%;
            height: 4px;
            background: #ddd;
            border-radius: 2px;
            overflow: hidden;
            margin-top: 5px;
        }
        #strength-fill {
            height: 100%;
            transition: width 0.3s ease;
            border-radius: 2px;
        }
        .weak { background: #ff4757; }
        .medium { background: #ffa502; }
        .strong { background: #2ed573; }
    </style>

    <script>
        const passwordField = document.getElementById('password');
        const strengthFill = document.getElementById('strength-fill');
        const strengthText = document.getElementById('strength-text');

        function getStrength(password) {
            let score = 0;
            if (password.length >= 8) score++;
            if (/[a-z]/.test(password)) score++;
            if (/[A-Z]/.test(password)) score++;
            if (/\\d/.test(password)) score++;
            if (/[@$!%*?&]/.test(password)) score++;
            return score;
        }

        passwordField.addEventListener('input', function() {
            const strength = getStrength(this.value);
            let strengthClass, text, width;
            switch (strength) {
                case 0:
                case 1: strengthClass = 'weak'; text = 'Weak'; width = '20%'; break;
                case 2:
                case 3: strengthClass = 'medium'; text = 'Medium'; width = '60%'; break;
                default: strengthClass = 'strong'; text = 'Strong'; width = '100%'; break;
            }
            strengthFill.className = strengthClass;
            strengthFill.style.width = width;
            strengthText.textContent = text;
        });
    </script>
{% endblock %}

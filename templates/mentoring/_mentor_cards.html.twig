{% for mentor in mentors %}
    {% set mentorUser = mentor.user %}
    <a href="{{ path('mentoring_show', {'id': mentor.id}) }}" class="mentor-card-link">
        <article class="mentor-card">
            <div class="mentor-card-top">
                <div class="mentor-avatar">
                    {% if mentorUser.profilePicture %}
                        <img src="{{ asset('uploads/profiles/' ~ mentorUser.profilePicture) }}" alt="{{ mentor.displayName }}">
                    {% else %}
                        <span>{{ mentor.displayName|first|upper }}</span>
                    {% endif %}
                </div>

                <div class="mentor-card-title-wrap">
                    <h4>{{ mentor.displayName }}</h4>
                    <p class="mentor-card-subtitle">{{ mentor.specialization ?: 'Faculty member' }}</p>

                    <div class="mentor-card-tags">
                        <span class="mentor-specialization-tag">{{ mentorUser.degreeName ?: mentorUser.degree ?: 'Education' }}</span>
                        <span class="mentor-specialization-tag">{{ mentor.specialization ?: 'Mentorship' }}</span>
                    </div>
                </div>
            </div>

            {% if mentor.bio %}
                <p class="mentor-description">{{ mentor.bio|slice(0, 140) ~ (mentor.bio|length > 140 ? '...' : '') }}</p>
            {% else %}
                <p class="mentor-description">No profile description provided.</p>
            {% endif %}

            <div class="mentor-card-footer">
                <span class="contact-info">{{ mentorUser.email }}</span>
                <div>
                    <span class="mentor-view-btn">View Profile</span>
                </div>
            </div>
        </article>
    </a>
{% else %}
    <div class="card-panel" style="grid-column: 1 / -1; text-align: center; padding: 3rem 2rem;">
        <p style="font-size: 1.1rem; color: #64748b;">No mentors found matching your search.</p>
        <p style="color: #94a3b8; margin-top: 0.5rem;">Try adjusting your search terms or filters.</p>
    </div>
{% endfor %}

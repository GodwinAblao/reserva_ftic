[variables]
APP_ENV = "prod"
COMPOSER_ALLOW_SUPERUSER = "1"
NIXPACKS_PHP_ROOT_DIR = "/app/public"
NIXPACKS_PHP_FALLBACK_PATH = "/index.php"
RAILPACK_PHP_ROOT_DIR = "/app/public"

[phases.setup]
nixPkgs = ['python311', 'python311Packages.pip', 'php83', 'php83Packages.composer']

[phases.build]
cmds = [
  "pip install -r analytics/requirements.txt --break-system-packages",
  "php bin/console asset-map:compile --env=prod"
]

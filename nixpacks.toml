[variables]
APP_ENV = "prod"
COMPOSER_ALLOW_SUPERUSER = "1"
NIXPACKS_PHP_ROOT_DIR = "/app/public"
NIXPACKS_PHP_FALLBACK_PATH = "/index.php"
RAILPACK_PHP_ROOT_DIR = "/app/public"

[phases.build]
cmds = ["php bin/console asset-map:compile --env=prod"]
